# Logging API

`boogr.py` defines configuration readers, text and traceback sanitization, the `Error` exception wrapper, the SQLite-backed `Logger`, and the `log_error` convenience function.

::: boogr
    options:
      members: true
      show_root_heading: false
      show_source: false
      separate_signature: true
      signature_crossrefs: true
      members_order: source
      filters:
        - "!^_"
